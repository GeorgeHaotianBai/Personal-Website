# George Bai academic website

This repository contains George Bai's academic website. It uses Hugo with a small custom theme and deploys through GitHub Pages.

## Local preview

Install Hugo Extended, then run:

```bash
hugo server --disableFastRender
```

Open the local URL printed by Hugo. See `UPDATING.md` for content editing instructions.

